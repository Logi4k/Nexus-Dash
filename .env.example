# Copy to `.env.local` for local development (Vite loads it automatically).
# For GitHub Actions desktop releases, add the same values as repository secrets:
#   VITE_SUPABASE_URL
#   VITE_SUPABASE_ANON_KEY

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SYNC_ENABLED=true
