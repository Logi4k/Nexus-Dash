# Nexus App — Changes Applied

**Date:** 5 April 2026
**Build:** CLEAN (0 errors, 6.78s)
**Tests:** 37 passed, 2 pre-existing failures (not caused by our changes)

---

## BUG FIXES (6 total)

### A1. CustomSelect.tsx — Mobile portal fixed
- `z-[99998]` → `z-[2147483647]` (matches DatePicker/TimePicker)
- Inner elements: `absolute` → `fixed` (escapes modal stacking context)
- Bottom-sheet: `absolute inset-x-0 bottom-0 rounded-t-[26px]` → `fixed inset-x-4 bottom-4 rounded-2xl`
- **File:** `src/components/CustomSelect.tsx`

### A2. Journal.tsx — Duplicate CustomSelect eliminated
- Deleted 171 lines of duplicated local `CustomSelect` function (lines 682-851)
- Added `import CustomSelect from "@/components/CustomSelect"` at top
- All 4 usage sites (filter, instrument, account, session selects) now use shared component
- **File:** `src/pages/Journal.tsx` — reduced from 3,191 to 3,021 lines

### A3-A5. Z-Index Architecture Standardised
| Component | Before | After |
|-----------|--------|-------|
| CommandPalette | z-[80] | z-[10000] |
| Journal lightbox | z-[320] | z-[10001] |
| CustomSelect mobile | z-[99998] + absolute | z-[2147483647] + fixed |
| Journal local CustomSelect | z-[140] + absolute | DELETED (uses shared component) |

**Files:** `CommandPalette.tsx`, `Journal.tsx`, `CustomSelect.tsx`

### A6. Build Verification
- Clean build: 0 TS errors, 0 warnings
- Journal bundle: 466.51 KB → 462.52 KB (4 KB saved from deduplication)

---

## REDESIGN CHANGES

### R1. CSS Design Tokens (index.css)
New utility classes added:
- `.display-num` — 32px/40px hero-scale financial numbers
- `.stat-display` — 24px medium-scale stat numbers  
- `.section-label` — 10px uppercase micro-headings
- `.card-primary` — elevated card with accent left border
- `.card-secondary` — standard content card
- `.card-tertiary` — minimal ghost card with dashed border
- `.section-gap` — consistent 32px/40px section spacing
- `.text-profit` / `.text-loss` — semantic P&L colour utilities

### R2. Typography Hierarchy
- Page title: 30px → 32px mobile / 36px desktop, weight 900→800
- Stat numbers: added letter-spacing -0.02em, weight bold→extrabold
- Hero P&L: 52px → 40px mobile / 56px desktop (responsive)
- KPI card values: text-[28px] → stat-display class (24px)
- Section labels: scattered `text-[10px]` patterns → unified `section-label` class
- CardTitle: text-sm → text-[15px] with tracking-tight
- CardDescription: text-[11px] → section-label class

### R3. Card System Hierarchy
- KPI cards: `card` → `card-primary` (accent left border + elevated shadow)
- Content sections: `card` → `card-secondary` (lighter shadow)
- Quick actions area: `card` → `card-secondary`
- New `.card-tertiary` available for minimal ghost elements

### R4. Colour & Contrast
- Text: tx-1 `#eef1f6` → `#f0f2f7` (slightly brighter)
- Text: tx-2 opacity 0.78 → 0.82 (better WCAG AA contrast)
- Accent: `#c8d0de` → `#a8b4cc` (blue-silver, more distinctive)
- Focus ring: added `box-shadow: 0 0 0 4px rgba(168,180,204,0.15)` glow

### R5. Mobile & Touch Experience
- Nav items: min-height 44px (WCAG touch target minimum)
- MobileNav bottom bar: min-w/h 36px → 44px
- MobileNav menu items: py-2 → py-3, px-2 → px-3
- Layout content padding: p-4/p-6 → p-5/p-7
- Button base: min-height 40px
- Tab pills: py-1.5 → py-2, min-height 36px

### R6. Sidebar
- Net Worth label: text-[10px] → section-label class
- Net Worth value: text-[28px] → stat-display class

---

## Files Modified (14 total)

| File | Changes |
|------|---------|
| `src/index.css` | +85 lines — new design tokens, upgraded existing classes |
| `src/components/CustomSelect.tsx` | Bug fix — z-index + fixed positioning |
| `src/components/CommandPalette.tsx` | Bug fix — z-index 80→10000 |
| `src/components/ui/card.tsx` | Typography upgrades |
| `src/components/Layout.tsx` | Content padding increase |
| `src/components/Sidebar.tsx` | Net Worth display upgrades |
| `src/components/MobileNav.tsx` | Touch target improvements |
| `src/pages/Journal.tsx` | -171 lines — removed duplicate, fixed lightbox z-index |
| `src/pages/Dashboard.tsx` | Hero P&L, KPI cards, card hierarchy |

---

## Pre-Existing Issues (Not Fixed)
- SettingsModal.tsx: 2 hardcoded `#a855f7` colours (test failure, not our change)
- propRules.test.ts: Challenge→Funded promotion test failure (pre-existing)
- Journal.tsx bundle still 462 KB (architectural — needs component extraction)
